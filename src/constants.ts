import { name } from "../package.json";

export const PROGRAM_NAME = name;
export const PROGRAM_DESCRIPTION = "A CLI for scaffolding components in React";

export const PackageManager = {
  NAME: "pnpm",
  INSTALL: "add",
};

export const FORM_LIB = "react-hook-form";
